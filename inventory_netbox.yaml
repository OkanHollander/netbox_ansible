---
plugin: netbox.netbox.nb_inventory
api_endpoint: "{{ lookup('env', 'NETBOX_API_URL')}}"
token: "{{ lookup('env', 'NETBOX_API_TOKEN') }}"
validate_certs: false

group_by:
  - device_roles
  - sites
